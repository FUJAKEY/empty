{
  "schemaVersion": 1,
  "id": "botmenu",
  "version": "${version}",
  "name": "Bot Menu Mod",
  "description": "Adds a bot management panel to the pause menu.",
  "authors": ["ChatGPT"],
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "client": ["com.example.botmenu.BotMenuMod"]
  },
  "mixins": [
    "botmenu.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}",
    "fabric-api": "*"
  }
}
